play.evolutions.autoApply = true

slick.dbs.default {
  profile = "slick.jdbc.PostgresProfile$"

  db {
    driver = "org.postgresql.Driver"
    url = ${DB_URL}
    user = ${DB_USER}
    password = ${DB_PASS}
  }
}
